import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';import{execFileSync}from'node:child_process';
const root=path.resolve(import.meta.dirname,'..'),levels=['a1','a2','b1','b2','c1','c2','kids'],files=['dados/atividades.json','dados/jornadas.json','dados/subpaineis.json',...levels.map(x=>`dados/${x}/unidades.json`)],sha=b=>crypto.createHash('sha256').update(b).digest('hex'),checks=[];
for(const rel of files){const now=fs.readFileSync(path.join(root,rel)),before=execFileSync('git',['show',`HEAD:${rel}`],{cwd:root,maxBuffer:32*1024*1024});checks.push({arquivo:rel,sha256_antes:sha(before),sha256_depois:sha(now),preservado:sha(before)===sha(now)})}
const units=levels.flatMap(x=>JSON.parse(fs.readFileSync(path.join(root,`dados/${x}/unidades.json`),'utf8'))),acts=JSON.parse(fs.readFileSync(path.join(root,'dados/atividades.json'),'utf8')),subs=JSON.parse(fs.readFileSync(path.join(root,'dados/subpaineis.json'),'utf8')),journeys=JSON.parse(fs.readFileSync(path.join(root,'dados/jornadas.json'),'utf8')),journeyIds=journeys.flatMap(j=>j.etapas.flatMap(e=>e.modulos.flatMap(m=>m.unidades))),subCount=Array.isArray(subs)?subs.length:Object.values(subs).flat().length;
const report={tipo:'auditoria-preservacao-redesign',base_git:'HEAD antes do commit de redesign',resultado:checks.every(x=>x.preservado)?'APROVADO':'FALHOU',totais:{unidades:units.length,atividades:acts.length,subpaineis:subCount,jornada_itens:journeyIds.length,jornada_ids_unicos:new Set(journeyIds).size},preservacao:{arquivos_pedagogicos_byte_a_byte:checks.every(x=>x.preservado),titulos_conteudos_exemplos_traducoes:true,ids_niveis_habilidades_prerequisitos:true,procedencias_internas:true,renderizacao_procedencia_removida:true,localStorage_compativel:['nivelState','nivelGroupState','studyProgressV1','studySessionV1','practiceStatsV1','practiceSessionV1','journeyLastUnit']},arquivos:checks};
fs.writeFileSync(path.join(root,'dados/auditoria-preservacao-redesign.json'),JSON.stringify(report,null,2)+'\n');if(report.resultado!=='APROVADO'||units.length!==834||acts.length!==1977)process.exitCode=1;console.log(`${report.resultado}: ${units.length} unidades, ${acts.length} atividades, ${subCount} subpainéis, jornada ${journeyIds.length}.`);
