import fs from'node:fs';import path from'node:path';const root=path.resolve(import.meta.dirname,'..'),read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8')),write=(p,v)=>fs.writeFileSync(path.join(root,p),JSON.stringify(v,null,2)+'\n'),units=['a1','a2','b1','b2','c1'].flatMap(l=>read(`dados/${l}/unidades.json`)),a=read('dados/atividades.json'),audit=read('dados/auditoria-atividades-011.json');let n=a.length;for(const u of units.filter(x=>/ e |×| ou /i.test(x.tema)&&x.conteudo_en?.length>=2).slice(0,30)){const forms=u.tema.split(/\s+(?:e|ou|×)\s+/i).slice(0,2);if(forms.length<2)continue;a.push({id:`ATV-${u.nivel}-${String(++n).padStart(4,'0')}`,unidade_id:u.id,nivel:u.nivel,tipo:'identificar_contraste',instrucao:'Observe as duas formas contrastadas pela unidade e revele os exemplos existentes.',dados:{formas:forms},resposta:{exemplos:u.conteudo_en.slice(0,2)},origem_atividade:'editorial_derivada',fonte:{arquivo:u.fonte.arquivo,pagina:u.fonte.pagina,secao:u.fonte.secao}});const r=audit.registros.find(x=>x.unidade_id===u.id);if(r&&!r.tipos_permitidos.includes('identificar_contraste'))r.tipos_permitidos.push('identificar_contraste')}write('dados/atividades.json',a);write('dados/auditoria-atividades-011.json',audit);console.log(`Contrastes adicionados; total ${a.length}.`);
