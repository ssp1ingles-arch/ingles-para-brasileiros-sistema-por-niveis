import fs from'node:fs';import path from'node:path';const r=path.resolve(import.meta.dirname,'..'),ns=['a1','a2','b1','b2','c1','c2','kids'],l=a=>JSON.parse(fs.readFileSync(path.join(r,a),'utf8')),w=(a,v)=>fs.writeFileSync(path.join(r,a),JSON.stringify(v,null,2)+'\n'),u=ns.flatMap(n=>l(`dados/${n}/unidades.json`)),a=l('dados/atividades.json'),j=l('dados/jornadas.json'),prev=l('dados/auditoria-estabilidade-jornada-023.json'),refs=j.flatMap(x=>x.etapas.flatMap(e=>e.modulos.flatMap(m=>m.unidades||[]))),presentes=new Set(refs),ids=new Set(u.map(x=>x.id)),cnt=new Map();refs.forEach(id=>cnt.set(id,(cnt.get(id)||0)+1));const por=ns.map(n=>{const pub=u.filter(x=>x.nivel.toLowerCase()===n),comp=pub.filter(x=>!presentes.has(x.id)).map(x=>({id:x.id,motivo:'Complementar preservada; o lote 024 não criou unidade.'}));return{nivel:n.toUpperCase(),publicadas:pub.length,presentes:pub.length-comp.length,complementares:comp}});w('dados/auditoria-atividades-024.json',{lote:'024',base_preservada:a.length,unidades_novas:0,atividades_novas:0,total:a.length,unidades_sem_atividade:66,justificativa:'Consolidação documental sem unidade nova ou atividade trivial.'});w('dados/auditoria-cobertura-jornada-024.json',{lote:'024',por_nivel:por,totais:{publicadas:u.length,presentes:presentes.size,complementares:u.length-presentes.size},ids_anteriores:prev.ids_preservados,ids_anteriores_preservados:prev.ids_preservados.filter(id=>presentes.has(id)),novos_posicionados:[],orfaos:[...presentes].filter(id=>!ids.has(id)),repetidos:[...cnt].filter(([,n])=>n>1),referencias_inexistentes:[...presentes].filter(id=>!ids.has(id)),etapas_modulos_afetados:[]});w('dados/auditoria-estabilidade-jornada-024.json',{lote:'024',ids_anteriores:prev.ids_preservados,ids_preservados:prev.ids_preservados.filter(id=>presentes.has(id)),ids_removidos:prev.ids_preservados.filter(id=>!presentes.has(id)),ids_adicionados:[],etapas_preservadas:true,modulos_preservados:true,ordem_preservada:true,localStorage:{formato_alterado:false,chaves_alteradas:false,favoritos_revisoes_compativeis:true,progresso_retomada_compativeis:true}});console.log(`Experiência 024: ${u.length} unidades, ${a.length} atividades, Jornada ${presentes.size}/${u.length}.`);
