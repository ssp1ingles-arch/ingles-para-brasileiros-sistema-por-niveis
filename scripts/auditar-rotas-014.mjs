import fs from 'node:fs';import path from 'node:path';import {chromium} from 'playwright-core';const raiz=path.resolve(import.meta.dirname,'..'),saida=path.join(raiz,'docs/evidencias/lote-014');fs.mkdirSync(saida,{recursive:true});
function htmls(d=raiz){return fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>['node_modules','.git'].includes(x.name)?[]:x.isDirectory()?htmls(path.join(d,x.name)):x.name.endsWith('.html')?[path.relative(raiz,path.join(d,x.name)).replaceAll('\\','/')]:[])}
const rotas=htmls().sort(),browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'}),audit=[];
for(const rota of rotas){const erros=[],page=await browser.newPage({viewport:{width:1440,height:1000}});page.on('console',m=>{if(m.type()==='error')erros.push(m.text())});page.on('pageerror',e=>erros.push(e.message));const res=await page.goto(`http://127.0.0.1:8000/${rota}`,{waitUntil:'networkidle'}),titulo=await page.title();const alvo=['index.html','estudar.html','praticar.html','jornada.html','niveis/a2/index.html','niveis/b1/index.html','niveis/c1/index.html','niveis/c2/index.html','niveis/kids/index.html'].includes(rota);if(alvo){const slug=rota.replace(/index\.html$/,'index').replaceAll('/','-').replace(/\.html$/,'');await page.screenshot({path:path.join(saida,`${slug}-desktop.png`),fullPage:true});await page.setViewportSize({width:390,height:844});await page.reload({waitUntil:'networkidle'});await page.screenshot({path:path.join(saida,`${slug}-mobile.png`),fullPage:true});}audit.push({caminho:rota,http_status:res?.status()||0,titulo,erros_console:[...new Set(erros)],desktop_celular:alvo});await page.close();}
await browser.close();fs.writeFileSync(path.join(raiz,'dados/auditoria-rotas-014.json'),JSON.stringify(audit,null,2)+'\n');const checks=[['11 rotas enumeradas',rotas.length===11],['HTTP 200',audit.every(x=>x.http_status===200)],['títulos presentes',audit.every(x=>x.titulo)],['console sem erros',audit.every(x=>x.erros_console.length===0)],['capturas alvo desktop/celular',audit.filter(x=>x.desktop_celular).length===9]].map(([teste,ok])=>({teste,resultado:ok?'APROVADO':'FALHOU'})),payload={total:checks.length,aprovados:checks.filter(x=>x.resultado==='APROVADO').length,resultados:checks};fs.writeFileSync(path.join(saida,'resultados-rotas-014.json'),JSON.stringify(payload,null,2)+'\n');console.log(`Rotas 014: ${audit.length}; ${payload.aprovados}/${payload.total}`);
