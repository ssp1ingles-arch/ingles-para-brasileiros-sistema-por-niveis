import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';const r=path.resolve(import.meta.dirname,'..'),src=(process.env.ARQUIVO_FONTE_DIR || path.resolve(import.meta.dirname,'../../Arquivo_Fonte')),ns=['a1','a2','b1','b2','c1','c2','kids'],l=a=>JSON.parse(fs.readFileSync(path.join(r,a),'utf8')),u=ns.flatMap(n=>l(`dados/${n}/unidades.json`)),a=l('dados/atividades.json'),t=l('dados/lote-019-triagem.json'),c=l('dados/auditoria-continuidade-lote018-019.json'),x=l('dados/mapeamento-fontes-extensas-019.json'),j=l('dados/auditoria-cobertura-jornada-019.json'),e=l('dados/auditoria-estabilidade-jornada-019.json'),s=l('dados/subpaineis.json'),out=[];function ok(n,v,d=''){out.push({teste:n,resultado:v?'APROVADO':'FALHOU',detalhe:d});if(!v)throw Error(n+': '+d)}ok('intervalo 1121-1162',t.sequenciais.length===42&&t.sequenciais[0].numero===1121&&t.sequenciais.at(-1).numero===1162);ok('41 duplicatas integrais',t.sequenciais.filter(z=>z.status==='duplicata'&&z.leitura_integral).length===41);ok('1162 canônica mapeada',t.sequenciais.at(-1).status==='integralmente classificada'&&t.sequenciais.at(-1).unidades_canonicas_relacionadas.length===104);ok('seis canônicas auditadas',c.fontes_canonicas.length===6&&c.fontes_canonicas.every(z=>z.unidades_destino.length&&z.conteudo_util_sem_destino.length===0));ok('dois exemplos preservados',c.exemplos_consolidados.length===2&&c.exemplos_consolidados.every(z=>z.removido_da_origem&&z.ocorrencia_canonica_preservada&&!z.traducao_ou_explicacao_exclusiva_perdida));ok('extensa por seção',x.fontes.length===1&&x.fontes[0].secoes.length===5&&x.fontes[0].unidades_destino.length===104);ok('IDs unidades únicos',new Set(u.map(z=>z.id)).size===u.length);ok('IDs atividades únicos',new Set(a.map(z=>z.id)).size===a.length);ok('95 subpainéis',s.length===95);ok('Kids CEFR',l('dados/kids/unidades.json').every(z=>z.nivel_cefr));ok('C2 justificado',l('dados/c2/unidades.json').every(z=>z.justificativa_c2));ok('Jornada fechada',j.totais.presentes+j.totais.complementares===u.length);ok('806 IDs preservados',e.ids_preservados.length===806&&e.ids_removidos.length===0);ok('sem órfãos/repetidos',!j.orfaos.length&&!j.repetidos.length&&!j.referencias_inexistentes.length);ok('localStorage estável',!e.localStorage.formato_alterado&&!e.localStorage.chaves_alteradas);ok('atividades preservadas',a.length===1977);ok('sem exemplo duplicado',(()=>{const v=u.flatMap(z=>(z.conteudo_en||[]).map(q=>q.trim().toLowerCase()));return new Set(v).size===v.length})());ok('hashes preservados',t.sequenciais.every(z=>crypto.createHash('sha256').update(fs.readFileSync(path.join(src,z.nome))).digest('hex')===z.hash_bruto));const payload={total:out.length,aprovados:out.filter(z=>z.resultado==='APROVADO').length,resultados:out};fs.mkdirSync(path.join(r,'docs/evidencias/lote-019'),{recursive:true});fs.writeFileSync(path.join(r,'docs/evidencias/lote-019/resultados-validacao-019.json'),JSON.stringify(payload,null,2)+'\n');console.log(`LOTE 019: ${payload.aprovados}/${payload.total}`);
