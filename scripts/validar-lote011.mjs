import fs from'node:fs';import path from'node:path';import crypto from'node:crypto';const root=path.resolve(import.meta.dirname,'..'),src=(process.env.ARQUIVO_FONTE_DIR || path.resolve(import.meta.dirname,'../../Arquivo_Fonte')),read=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8')),sha=b=>crypto.createHash('sha256').update(b).digest('hex'),e=[],t=read('dados/lote-011-triagem.json');for(const x of[...t.sequenciais,...t.amostragem_direcionada])if(sha(fs.readFileSync(path.join(src,x.nome)))!==x.hash_bruto)e.push(`Fonte modificada ${x.nome}`);const units=['a1','a2','b1','b2','c1','c2','kids'].flatMap(l=>read(`dados/${l}/unidades.json`)),ids=new Set(units.map(x=>x.id)),a=read('dados/atividades.json');for(const x of a){if(!ids.has(x.unidade_id))e.push(`Atividade órfã ${x.id}`);if(!Object.keys(x.resposta).length)e.push(`Resposta vazia ${x.id}`);if(x.tipo==='multipla_escolha')e.push(`Distrator proibido ${x.id}`)}const s=read('dados/auditoria-estabilidade-jornada-011.json');if(s.ids_removidos.length)e.push('Jornada instável');const tests=read('docs/evidencias/lote-011/resultados-lote011.json');if(tests.aprovados!==tests.total)e.push('Testes falharam');if(e.length){console.error(e.join('\n'));process.exit(1)}console.log(`LOTE 011 OK: ${a.length} atividades, ${tests.aprovados}/${tests.total} testes novos, jornada estável.`);
